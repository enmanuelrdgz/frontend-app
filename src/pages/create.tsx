import CreateSurveyCard from "@/components/CreateSurveyCard"

const Create: React.FC<void> = () => {
    return (
        <main style={{display: "flex", justifyContent: "center", alignItems: "center"}}>
            <CreateSurveyCard />
        </main>
    )
}

export default Create;